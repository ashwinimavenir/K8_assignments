Write a common use-case, where you will use a daemon set instead of replica set.

DaemonSets can improve the performance of a Kubernetes cluster by distributing maintenance tasks and support services via deploying Pods across all nodes. They are well suited for long-running services like monitoring or log collection. To run a daemon for node monitoring on every note, such as Prometheus Node Exporter, collectd, or Datadog agent.
